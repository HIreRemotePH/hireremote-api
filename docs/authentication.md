# Authentication

The HireRemote API uses secure API keys for authentication.

Requests to protected endpoints include the header:

Authorization: Bearer YOUR_API_KEY

API keys will be issued to approved partners integrating with HireRemote.ph.
